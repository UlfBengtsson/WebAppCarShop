@model CarIndexViewModel
@{
    ViewData["Title"] = "Car Index";
}

<h1>Index</h1>

<a asp-action="Create">Create</a>

<form asp-action="Index" method="post">
    <label asp-for="FilterText"></label>
    <input asp-for="FilterText" />
    <input type="submit"  value="Filter by Brand" />
</form>

@*<table>
    <thead>
        <tr>
            <th>Brand</th>
            <th>Model</th>
            <th>Price</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.CarList)
	    {
        <tr>
            <td>@item.Brand</td>
            <td>@item.ModelName</td>
            <td>@item.Price</td>
            <td><a asp-action="Details" asp-route-id="@item.Id">Details</a></td>
        </tr>
	    }
    </tbody>
    
</table>*@

<hr />

<!--Table-->
<div class="row">
    <div class="col-12">
        <!--head-->
        <div class="row">
            <!--Brand-->
            <div class="col-4">
                <h5>Brand</h5>
            </div>
            <!--Model-->
            <div class="col-4">
                <h5>Model</h5>
            </div>
            <!--Price-->
            <div class="col-2">
                <h5>Price</h5>
            </div>
            <!--Action-->
            <div class="col-2">
                <h5>Action</h5>
            </div>
        </div>
        <!--body-->
        @foreach (var item in Model.CarList)
        {

            <partial name="../Shared/_ACarRowPartcialView" model="item" />

            <!--<div class="row">-->
                <!--Brand-->
                <!--<div class="col-4">
                    <p>@item.Brand</p>
                </div>-->
                <!--Model-->
                <!--<div class="col-4">
                    <p>@item.ModelName</p>
                </div>-->
                <!--Price-->
                <!--<div class="col-2">
                    <p>@item.Price</p>
                </div>-->
                <!--Action-->
                <!--<div class="col-2">
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info" >Details</a>
                </div>
            </div>-->
        }
    </div>
</div>